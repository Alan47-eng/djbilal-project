[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'printf \"window.__ENV__ = { VITE_API_BASE_URL: \\\"%s\\\" };\\\\n\" \"${VITE_API_BASE_URL:-https://djbilal-backend.railway.app}\" > dist/env.js && npm run start -- --port ${PORT:-4173}'"
